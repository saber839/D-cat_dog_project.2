# 复制为 .env 或在系统环境变量中设置（Windows PowerShell 示例见下方）
# PowerShell: $env:LLM_API_KEY="your_key_here"

# DeepSeek API Key（留空则使用规则模式，不调用大模型）
LLM_API_KEY=

# LLM API 地址（默认 DeepSeek）
LLM_BASE_URL=https://api.deepseek.com/v1/chat/completions

# 模型权重路径（默认：项目根目录/models/best_cnn_model.pth）
MODEL_PATH=
